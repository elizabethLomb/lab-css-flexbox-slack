window.addEventListener("click", (event => {
  const toggle = document.getElementById('navbar-toggler')
  const mainNav = document.getElementById('mainNav')

  if(toggle === 'collapse'){
    return true
  }

  //event : mainNav.classList.add('show')
}))